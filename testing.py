#from potflow import potflowvis
import matplotlib.pyplot as plt
import numpy as np

# fig, ax = potflowvis([["ufl", [0,0], 20],["dbl", [0,0], 200]], levels=10)
# fig.canvas.draw()
# plt.pause(0.1) 

# # Keep the script alive so you can see the plot
# input("Press Enter to close...")
# a = np.array([])
# b = np.array([0,3])
# np.vstack((a,b))
# print(np.vstack((a,b)))
for i in range(3):
    print(i)